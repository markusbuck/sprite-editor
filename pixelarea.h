#ifndef PIXELAREA_H
#define PIXELAREA_H

#include <QWidget>

class PixelArea : public QWidget
{
    Q_OBJECT
public:
    explicit PixelArea(QWidget *parent = nullptr);

signals:
};

#endif // PIXELAREA_H
